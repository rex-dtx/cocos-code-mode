# Verify API cc3x7 — doi chieu docs goc + cc2x4

**Ngay:** 2026-08-21 · **Branch:** `cc-3x7` (45 tools) vs `cc-2x` (53 tools) · **Registry:** `docs/cc-3x7-message-registry.json` (416 msg, 20 module, 3.7.3 dump)

## 1. Coverage message

| Nhom | cc3x7 dung | Registry | Thieu | Trang thai |
|------|-----------|----------|-------|------------|
| scene | 95+ | 191 | 0 chinh | OK |
| asset-db | 23 | 45 | 1 (`query-asset-thumbnail`) | fallback renderer + previewManage |
| builder | 5 | 44 | 0 | OK (buildManage) |
| program | 1 (+2 fallback) | 4 | 2 (`open-program`, `open-url`) | FIX: `open-program`->`execute`, `open-url`->`execFile` http(s) only |
| project | 1 | 15 | 1 (`set-config`) | KHONG map — bao unsupported (dung, map bua = ghi sai key im lang) |
| preview/engine | 3 | 25 | 0 | OK |
| **Tong** | **131 distinct `Editor.Message.request`** | **416** | **3 vang mat** | **da xu ly het** |

Signature drift da fix: `asset-db/query-assets` (`{pattern}` object vs `string` o 3.7.3) qua `queryAssetsCompat` (try object -> catch -> string + re-filter client-side). `query-asset-info`/`query-uuid`/`query-url`/`query-path` chi nhan url/uuid dung — code da dung dung.

## 2. Tool surface — 45 vs 53

**cc3x7 (45 = 35 standalone + 10 consolidated):**
`animationEdit/Query`, `assetCreate/GetTree/GetAtPath/ResolvePath/FindReferences/Query/SaveContent/GetAvailableUrl/Import/Operate`, `assetDbQuery`, `materialQuery`, `buildManage`, `callComponentMethod`, `editorEnvInfo/GetLogs/History/Query/Select/Viewport`, `findNodesByAsset/WithMissingAssets`, `inspectorGet/Set/GetDefinition`, `listComponentClasses/Methods`, `nodeClipboard/ComponentManage/ComponentsGet/Create/CreatePrimitive/GetAtPath/GetAvailableComponentTypes/GetTree/Operate/Reset`, `previewManage`, `programManage`, `projectManage`, `propertyArrayElement`, `sceneGetInfo/Manage`

**Chi co o cc3x7 (30):** toan bo consolidated + `assetFindReferences`, `assetDbQuery`, `materialQuery`, `editorViewport/History`, `findNodes*`, `inspector*`, `listComponentClasses`, `propertyArrayElement`, `sceneGetInfo` — nhom nay 3.x dac thu, 2x khong co message tuong ung.

**Chi co o cc2x4 (38):** `assetCreateFolder/Delete/ExchangeUuid/Move/ReadContent/Refresh/SaveMeta/WriteContent`, `assetResolve` (giau), `batchSetProperties`, `componentQuery`, `editorOperate/Undo/ListTypes`, `nodeDuplicate/Move/Query/Remove/SetProperty/SetPropertyUndo`, `prefabSync`, `probeSceneIpc`, `sceneCreateNodeHL/SetPropertyHL/New/Open/Script/Snapshot`, `previewGetUrl/OpenInBrowser`, `assetGetPreview`, `editorGetScenePreview`. Da so la 2x-only: `Editor.assetdb` sync + `Editor.Selection` 18 methods + `scene-script` IPC + `scene:*` mutation timeout.

## 3. Consolidated 2.0.x — OK

`68 -> 51 -> 45` (10 consolidated thay 26 legacy). Legacy method bodies giu lai (khong `@utcpTool`) de consolidated delegate — dung. `code-mode-references.d.ts` da drop 26, `scripts/smoke-utcp.js` expects 45, `docs/consolidated-migration.md` codemod du.

3 message vang mat van giu implementation de delegation khong vo: `query-asset-thumbnail` (co sharp fallback), `program:open-url`/`open-program`/`project:set-config` da co try/catch ro rang.

## 4. Verify so voi docs goc + cc2x4 — ket luan

**Dung, khong can sua gap:**
- `queryAssetsCompat`, `program fallback`, `project set-config unsupported`, `getParent` via `query-current-scene`, `captureScreenshot` JPEG magic `/9j/` guard, `nodeGetTree` maxDepth/maxNodes/fields, `previewManage` imageSize `{width,height}` — da dung.
- `scene-tools` prefab-edit root unwrap, `component-tools` compType fallback `__type__/cid` — da dung.
- `editorViewport`, `materialQuery`, `assetDbQuery` — 2x khong co tuong ung, khong can doi chieu.

**Gap dang chu y (khong phai bug, nhung can quyet dinh co mo rong):**

| Candidate | Danh gia | Khuyen nghi |
|-----------|----------|-------------|
| `assetResolvePath` ngheo vs `assetResolve` giau cua 2x (exists/type/mount/relative/backup/urlToFspath...) | 3.7.3 registry co `query-asset-info`/`query-path`/`query-url`/`query-uuid` du de mo rong | **Mo rong `assetResolvePath` hoac them `assetDbQuery` op** neu agent can — de backlog, khong chan |
| `assetExchangeUuid` (ca 2 phien deu co `exchangeUuid`) | 2x co tool, 3x7 chua expose | **Them vao `assetOperate`** (`exchange_uuid`) khi can — 1 dong |
| `editorSelect` 4 ops vs 2x 18 methods (hover/context/patch/filter/confirm) | 3.7 `Editor.Selection` la `Editor.Message('selection', ...)` khac 2x sync, nhung 3.7 van co hover/context | **De backlog** — chi mo rong khi agent thuc su can hover/context |
| `batchSetProperties` multi-node | 3x7 `inspectorSet` chi single-target multi-path | **Khong port** — 3x7 co `set-property` + `snapshot` per node, batch multi-node it dung; can thi them `inspectorSet` batch mode sau |
| `sceneScript`/`probeSceneIpc`/`prefabSync`/`sceneSnapshot` | 2x-only (IPC JSON-only) | **Khong port** |
| `buildManage` control `break/remove/recompile` | 2x khong co Build | **Giu nguyen** — 3x7 da du |

## 5. Checklist verify tiep theo (can runtime 3.7.3)

1. Reload editor de `dist/` moi nhat co hieu luc, `curl /build-info` xac nhan, roi smoke lai nhom da sua truoc do treo (`assetGetPreview` sharp vs preview panel, `previewManage` scene_preview 256 vs `{width,height}`).
2. Test 3 fallback sau reload: `programManage open` -> `execute`, `open_url` -> `execFile` (http/https only), `projectManage set` bao unsupported.
3. `buildManage` trigger/control tren project co build task that (hien smoke chi test `query-tasks-info`/`query-worker-ready`).
4. Neu mo rong `assetResolvePath`/`assetExchangeUuid`/`editorSelect` — them smoke case tuong ung truoc khi merge.

## 6. Nguon doi chieu

- Registry 3.7.3: `docs/cc-3x7-message-registry.json` (20 module, 416 msg)
- Drift & strategy: `docs/cocos-3x7-api-drift.md` (da chot branch doc lap)
- Smoke 3.7.3 that: `plans/reports/verifier-260806-1520-3x7-smoke.md` (build cu, 1 case treo do chua reload)
- 2x verified: `cc-2x:docs/api-2x-reference.md` + `cocos-2x-api-notes.md` + `forum-92605-cocos-2x-api.md` (Probe 4: 14 scene:* mutation timeout -> giu scene-script)
- Consolidated: `docs/consolidated-migration.md` + `CHANGELOG.md` 2.0.x

**Ket luan:** API cc3x7 hien tai dung voi 3.7.3 registry, 3 drift chinh da co compat guard, 3 message vang mat da xu ly dung nguyen tac (bao loi thay vi map bua). Khong co bug chan. Gap vs cc2x4 la chu dong do kien truc khac (2x sync assetdb/selection + scene-script), khong phai thieu sot.
