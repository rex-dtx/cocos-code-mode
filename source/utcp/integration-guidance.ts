/**
 * Agent-facing integration guidance for expansion candidates.
 *
 * Keep this registry synchronized with docs/tool-portfolio-candidates.json.
 * It is intentionally concise: selection intent, route, prerequisites, and
 * the required verification step. Public UTCP descriptions append this text.
 */
export const expansionIntegrationGuidance: Record<string, string> = {
    "animationGraphCreate": "Use when: Create a valid native graph asset, prove import/read-back, and roll back on failure. Route: asset preset plus graph validator. Preconditions: animation-graph-format, asset-create. Call the typed schema; verify the returned postcondition and evidence limits.",
    "animationGraphEdit": "Use when: Persistent bounded graph mutations independent of panel selection state. Route: non-panel IPC or versioned serializer. Preconditions: animation-graph-write. Call the typed schema; verify the returned postcondition and evidence limits.",
    "animationGraphInspect": "Use when: Typed layers, states, transitions, variables and masks from native serialized data. Route: versioned graph reader. Preconditions: animation-graph-format. Call the typed schema; verify the returned postcondition and evidence limits.",
    "animationGraphPreview": "Use when: Run graph and observe transitions/events in a named runtime session. Route: runtime session. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "animationGraphValidate": "Use when: Validate unreachable states, missing clips, invalid variables and transition conditions. Route: graph semantic validator. Preconditions: animation-graph-format. Call the typed schema; verify the returned postcondition and evidence limits.",
    "animationRetargetValidate": "Use when: Check bounded skeleton, joint and clip compatibility without claiming automatic retargeting; fail closed when metadata is unavailable. Route: model and clip metadata validator. Preconditions: model-skeleton-metadata. Call the typed schema; verify the returned postcondition and evidence limits.",
    "assetBundleValidate": "Use when: Validate bundle metadata and actual runtime load/release behavior. Route: asset-db plus runtime session. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "assetCatalogManifest": "Use when: Bounded deterministic manifest with dependencies, hashes and explicit exclusions. Route: asset-db query and hashing. Preconditions: asset-query-contract. Call the typed schema; verify the returned postcondition and evidence limits.",
    "assetCompressionConfigure": "Use when: Platform-aware compression validation plus generated-output evidence. Route: importer registry and build artifact check. Preconditions: importer-schema, asset-reimport, build-artifact. Call the typed schema; verify the returned postcondition and evidence limits.",
    "assetImportSettingsSet": "Use when: Typed importer mutation with preflight, reimport and output read-back. Route: importer registry. Preconditions: importer-schema, asset-reimport. Call the typed schema; verify the returned postcondition and evidence limits.",
    "assetImporterAudit": "Use when: Typed importer-specific schema and normalized settings, not raw metadata. Route: importer registry. Preconditions: importer-schema. Call the typed schema; verify the returned postcondition and evidence limits.",
    "assetSceneUsageAudit": "Use when: Evidence-backed unreferenced candidates with roots, confidence and dynamic-load caveats. Route: Cocos Graph analysis. Preconditions: graph-v4. Call the typed schema; verify the returned postcondition and evidence limits.",
    "audioAssetCompatibilityAudit": "Use when: Validate format, decode/load mode, duration and target compatibility. Route: asset importer and platform validator. Preconditions: audio-import-contract. Call the typed schema; verify the returned postcondition and evidence limits.",
    "audioPlaybackControl": "Use when: Play, pause, stop and seek on a named runtime source with state confirmation. Route: runtime audio adapter. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "audioPlaybackObserve": "Use when: Bounded playback progress/state observation with activation errors. Route: runtime audio adapter. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "audioSourceAudit": "Use when: Normalized AudioSource and clip state including load mode and platform constraints. Route: audio adapter. Preconditions: audio-schema. Call the typed schema; verify the returned postcondition and evidence limits.",
    "audioSourceConfigure": "Use when: Typed bounded source configuration with clip compatibility read-back. Route: audio adapter plus inspector service. Preconditions: audio-schema, mutation-workflow. Call the typed schema; verify the returned postcondition and evidence limits.",
    "buildOutputAudit": "Use when: Verify terminal build outputs, expected files, hashes and target metadata. Route: bounded artifact inspector. Preconditions: build-job. Call the typed schema; verify the returned postcondition and evidence limits.",
    "buildPresetAudit": "Use when: Validate target-specific required/unsupported build options before dispatch. Route: build schema adapter. Preconditions: build-schema. Call the typed schema; verify the returned postcondition and evidence limits.",
    "buildTargetLaunch": "Use when: Launch the exact produced artifact on a declared target, not preview fallback. Route: target-specific launcher. Preconditions: build-job, named-target. Call the typed schema; verify the returned postcondition and evidence limits.",
    "buildTargetSmoke": "Use when: Interact with and observe the launched artifact on its declared target. Route: runtime session plus target launcher. Preconditions: runtime-session, named-target. Call the typed schema; verify the returned postcondition and evidence limits.",
    "lightBakeManage": "Use when: Finite bake job lifecycle and output asset verification. Route: version-gated bake API. Preconditions: light-bake-job-api. Call the typed schema; verify the returned postcondition and evidence limits.",
    "localizationInspect": "Use when: Discover enabled package, locales, compiled tables and resource mappings. Route: localization package adapter. Preconditions: localization-package-api. Call the typed schema; verify the returned postcondition and evidence limits.",
    "localizationPreview": "Use when: Switch locale with acknowledged runtime restart and visual/read-back evidence. Route: runtime localization adapter. Preconditions: runtime-session, localization-package-api. Call the typed schema; verify the returned postcondition and evidence limits.",
    "localizationTableEdit": "Use when: Typed persistent table mutation through documented package API. Route: localization package adapter. Preconditions: localization-package-api. Call the typed schema; verify the returned postcondition and evidence limits.",
    "localizationValidate": "Use when: Detect missing keys, fallback gaps, glyph/font and layout risks in compiled resources. Route: asset and UI validator. Preconditions: localization-resource-contract. Call the typed schema; verify the returned postcondition and evidence limits.",
    "materialEdit": "Use when: Typed bounded material property mutation through the registered importer with native material read-back; rendered visual postcondition remains unverified. Route: material adapter plus importer. Preconditions: material-schema, asset-reimport, visual-harness. Call the typed schema; verify the returned postcondition and evidence limits.",
    "modelImportConfigure": "Use when: Typed import, reimport and mesh/skeleton/LOD output validation. Route: model importer adapter. Preconditions: importer-schema, asset-reimport, model-skeleton-metadata. Call the typed schema; verify the returned postcondition and evidence limits.",
    "particleConfigure": "Use when: Typed module/curve mutation with persistent read-back, not panel scraping. Route: component metadata adapter. Preconditions: particle-schema. Call the typed schema; verify the returned postcondition and evidence limits.",
    "particleInspect": "Use when: Normalized modules, curves, gradients, assets and current configuration. Route: particle adapter. Preconditions: particle-schema. Call the typed schema; verify the returned postcondition and evidence limits.",
    "particlePlayback": "Use when: Play, stop, clear and completion observation in a named runtime session. Route: runtime particle adapter. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "particleValidate": "Use when: Validate ranges, required textures/materials/modules and backend limits. Route: particle semantic validator. Preconditions: particle-schema. Call the typed schema; verify the returned postcondition and evidence limits.",
    "physics2dCompatibilityAudit": "Use when: Reject unsupported backend/component/joint/matrix combinations. Route: physics-2d semantic validator. Preconditions: physics-2d-backend-matrix. Call the typed schema; verify the returned postcondition and evidence limits.",
    "physics2dConfigure": "Use when: Backend-aware settings and collision matrix mutation with read-back. Route: version-gated project settings. Preconditions: project-settings-write. Call the typed schema; verify the returned postcondition and evidence limits.",
    "physics2dCreateBody": "Use when: Compound node/body/collider creation with backend preflight and persistence. Route: compound scene workflow. Preconditions: physics-2d-backend-matrix, compound-scene-workflow. Call the typed schema; verify the returned postcondition and evidence limits.",
    "physics2dCreateJoint": "Use when: Typed joint creation with endpoint validation and Builtin rejection. Route: compound scene workflow. Preconditions: physics-2d-backend-matrix, compound-scene-workflow. Call the typed schema; verify the returned postcondition and evidence limits.",
    "physics2dQuery": "Use when: Runtime raycast/contact/overlap queries identified by backend and session. Route: runtime physics adapter. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "physics2dTopologyAudit": "Use when: Normalized backend, body, collider, group and joint topology. Route: physics-2d adapter. Preconditions: physics-2d-backend-matrix. Call the typed schema; verify the returned postcondition and evidence limits.",
    "physics3dConfigure": "Use when: Backend-aware settings mutation with version/platform read-back. Route: version-gated project settings. Preconditions: project-settings-write. Call the typed schema; verify the returned postcondition and evidence limits.",
    "physics3dCreateBody": "Use when: Create one PhysX rigid-body node with one BoxCollider and bounded Vec3 size, with backend preflight, read-back and rollback. Route: compound scene workflow. Preconditions: physics-3d-backend-matrix, compound-scene-workflow. Call the typed schema; verify the returned postcondition and evidence limits.",
    "physics3dCreateJoint": "Use when: Typed constraint creation with backend-specific validation. Route: compound scene workflow. Preconditions: physics-3d-backend-matrix, compound-scene-workflow. Call the typed schema; verify the returned postcondition and evidence limits.",
    "physics3dQuery": "Use when: Runtime raycast, sweep, contact and trigger queries tied to backend/session. Route: runtime physics adapter. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "physics3dTopologyAudit": "Use when: Normalized backend, rigidbody, collider, material and constraint topology. Route: physics-3d adapter. Preconditions: physics-3d-backend-matrix. Call the typed schema; verify the returned postcondition and evidence limits.",
    "physics3dValidate": "Use when: Reject unsupported Builtin/cannon/PhysX shapes, bodies, materials and constraints. Route: physics-3d semantic validator. Preconditions: physics-3d-backend-matrix. Call the typed schema; verify the returned postcondition and evidence limits.",
    "prefabOverrideDiff": "Use when: Structural live-instance versus prefab-source override diff with stable identities. Route: scene plus asset comparison. Preconditions: live-prefab-identity, asset-query-contract. Call the typed schema; verify the returned postcondition and evidence limits.",
    "prefabReferenceAudit": "Use when: Detect broken or ambiguous nested prefab and override references after reload. Route: scene and asset graph validator. Preconditions: live-prefab-identity, graph-v4. Call the typed schema; verify the returned postcondition and evidence limits.",
    "prefabVariantCreate": "Use when: Rejected concept: Creator has no native prefab-variant asset. Route: none. Preconditions: none recorded. Call the typed schema; verify the returned postcondition and evidence limits.",
    "referenceImageManage": "Use when: Finite persistent reference-image lifecycle with transform read-back. Route: version-gated editor IPC. Preconditions: reference-image-ipc. Call the typed schema; verify the returned postcondition and evidence limits.",
    "renderConfigurationApply": "Use when: Atomic camera/light/fog/skybox configuration with scene read-back. Route: compound scene workflow. Preconditions: render-pipeline-schema, compound-scene-workflow, visual-harness. Call the typed schema; verify the returned postcondition and evidence limits.",
    "renderDiagnosticsCollect": "Use when: Target-identified frame/render sampling with bounded results. Route: runtime render diagnostics. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "renderPipelineInspect": "Use when: Bounded native pipeline read-back through scene/query-render-pipeline; normalized compatibility diagnostics remain out of scope. Route: render adapter. Preconditions: render-pipeline-schema. Call the typed schema; verify the returned postcondition and evidence limits.",
    "runtimeScenarioAssert": "Use when: Independent state/image/event assertions rather than request echo. Route: runtime observation adapter. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "runtimeScenarioRun": "Use when: Execute finite named steps only; no code string or general method dispatch. Route: scenario compiler to typed primitives. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "runtimeSessionLifecycle": "Use when: Attach, identify, stop and reset finite game-view/browser/simulator sessions. Route: runtime session foundation. Preconditions: preview-lifecycle, runtime-transport. Call the typed schema; verify the returned postcondition and evidence limits.",
    "runtimeStateObserve": "Use when: Bounded allow-listed observations tied to session identity and target kind. Route: runtime session foundation. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "shaderValidate": "Use when: Return real effect importer/compiler diagnostics tied to source locations. Route: effect importer diagnostics. Preconditions: effect-compiler-diagnostics. Call the typed schema; verify the returned postcondition and evidence limits.",
    "skeletalAnimationConfigure": "Use when: Configure bounded serialized playOnLoad/useBakedAnimation fields with component read-back and rollback; skin, mix, and socket setup remain unimplemented. Route: skeletal adapter plus inspector service. Preconditions: skeletal-extension-detection, mutation-workflow. Call the typed schema; verify the returned postcondition and evidence limits.",
    "skeletalAnimationEvents": "Use when: Bounded ordered event observation tied to one playback session. Route: runtime event adapter. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "skeletalAnimationInspect": "Use when: Typed skins, tracks, sockets, animations and extension availability. Route: Spine/DragonBones asset adapter. Preconditions: skeletal-extension-detection. Call the typed schema; verify the returned postcondition and evidence limits.",
    "skeletalAnimationPlay": "Use when: Bounded play/queue/stop contract on a named runtime target. Route: runtime skeletal adapter. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "skeletalAnimationValidate": "Use when: Validate skeleton/atlas/textures, named animations, skins and runtime extension support. Route: asset and component validator. Preconditions: skeletal-extension-detection. Call the typed schema; verify the returned postcondition and evidence limits.",
    "spriteAtlasConfigure": "Use when: Configure, reimport and verify generated atlas/frame outputs rather than only writing importer meta. Route: importer service plus output validator. Preconditions: importer-schema, asset-reimport. Call the typed schema; verify the returned postcondition and evidence limits.",
    "terrainCreate": "Use when: Create and attach a valid terrain asset/component with reopen proof. Route: asset preset plus compound scene workflow. Preconditions: terrain-schema, asset-create, compound-scene-workflow. Call the typed schema; verify the returned postcondition and evidence limits.",
    "terrainEdit": "Use when: Persistent bounded height/layer mutation through a supported API. Route: version-gated terrain API. Preconditions: terrain-write-api. Call the typed schema; verify the returned postcondition and evidence limits.",
    "terrainInspect": "Use when: Typed terrain dimensions, layers, materials and asset references. Route: terrain asset adapter. Preconditions: terrain-schema. Call the typed schema; verify the returned postcondition and evidence limits.",
    "tilemapCreate": "Use when: Creator imports TMX; it does not provide a native map authoring API. Route: external Tiled workflow. Preconditions: none recorded. Call the typed schema; verify the returned postcondition and evidence limits.",
    "tilemapInspect": "Use when: Typed TMX map, layer, object and tileset inspection after Creator import. Route: asset parser plus imported-data query. Preconditions: tmx-import-contract. Call the typed schema; verify the returned postcondition and evidence limits.",
    "tilemapLayerEdit": "Use when: Persistent layer mutation without corrupting source TMX. Route: supported source-format editor only. Preconditions: tmx-roundtrip. Call the typed schema; verify the returned postcondition and evidence limits.",
    "tilemapObjectEdit": "Use when: Persistent typed object-layer mutation with TMX round-trip. Route: supported source-format editor only. Preconditions: tmx-roundtrip. Call the typed schema; verify the returned postcondition and evidence limits.",
    "tilemapValidate": "Use when: Validate tileset links, gids, layer dimensions and imported references. Route: TMX plus asset-db validator. Preconditions: tmx-import-contract. Call the typed schema; verify the returned postcondition and evidence limits.",
    "tweenSequenceControl": "Use when: Runtime control would target generated project code, not a native asset. Route: runtime project-code workflow. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "tweenSequenceCreate": "Use when: Creator has no native tween sequence asset. Route: project-code workflow. Preconditions: none recorded. Call the typed schema; verify the returned postcondition and evidence limits.",
    "tweenSequenceInspect": "Use when: No persistent native sequence exists to inspect. Route: project-code workflow. Preconditions: none recorded. Call the typed schema; verify the returned postcondition and evidence limits.",
    "tweenSequenceValidate": "Use when: Validation belongs to TypeScript compile/runtime behavior. Route: diagnostics plus runtime session. Preconditions: runtime-session. Call the typed schema; verify the returned postcondition and evidence limits.",
    "uiAccessibilityAudit": "Use when: Typed constraint tree and computed world rectangles, not a generic property dump. Route: scene query plus layout normalization. Preconditions: scene-query-contract. Call the typed schema; verify the returned postcondition and evidence limits.",
    "uiFocusNavigation": "Use when: Atomic bounded layout constraint application with preflight, one snapshot and read-back. Route: shared inspector mutation service. Preconditions: scene-query-contract, mutation-workflow. Call the typed schema; verify the returned postcondition and evidence limits.",
    "uiFormValidationBind": "Use when: Create labeled input hierarchy, focus order and validation binding with functional read-back. Route: compound scene workflow. Preconditions: compound-scene-workflow. Call the typed schema; verify the returned postcondition and evidence limits.",
    "uiResponsivePreview": "Use when: Render and compare one scene across declared resolutions with target-specific evidence. Route: runtime session plus screenshot adapter. Preconditions: runtime-session, preview-resolution. Call the typed schema; verify the returned postcondition and evidence limits.",
    "uiSafeAreaInspect": "Use when: Detect clipping, overlap, missing anchors and inconsistent safe-area constraints. Route: scene geometry validator. Preconditions: scene-query-contract. Call the typed schema; verify the returned postcondition and evidence limits.",
    "uiVirtualListCreate": "Use when: Create and verify the complete view-mask-content hierarchy, not one prefab node. Route: compound scene workflow. Preconditions: compound-scene-workflow. Call the typed schema; verify the returned postcondition and evidence limits.",
};
