{
  "name": "cocos-graph",
  "version": "1.0.0",
  "type": "module",
  "description": "Offline Creator 2.4 fire/prefab graph index — shard-per-bundle, plain JSON + Maps",
  "bin": {
    "cocos-graph": "./bin/cocos-graph.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
